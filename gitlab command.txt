git
git config -l

<!-- Login -->
- git --global user.name / user.email "..."

<!--Folder-->
git clone [link] > Lokasi folder yang akan dibuat
cd = ganti lokasi folder dari cmd
ls detail folder


<!--MAIN COMMAND-->
git add -A (menambahkan ubahan)
git commit -m "..." (msg)
git push origin master (kirim > server)
git pull (pembaruan file/pengambilan)

<!--BRANCH-->
git branch (cek)
git branch ... (buat branch baru)
git checkout ... (pindah branch)